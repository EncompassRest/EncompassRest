name: Publish
on:
  workflow_run:
    workflows: ["Build"]
    branches:
      - master
    types:
      - completed

jobs:
  publish:
    runs-on: windows-latest
    env:
      CONFIGURATION: Release
    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        source-url: https://api.nuget.org/v3/index.json
      env:
        NUGET_AUTH_TOKEN: ${{ secrets.NUGET_API_KEY }}

    - name: Restore NuGet Packages
      run: dotnet restore src/EncompassApi/EncompassApi.csproj

    - name: Build Solution
      run: dotnet build -c $env:CONFIGURATION src/EncompassApi/EncompassApi.csproj

    - name: Push NuGet Package
      run: dotnet nuget push --no-symbols --skip-duplicates --api-key ${{ secrets.NUGET_API_KEY }} "src/EncompassApi/bin/Release/**/*.nupkg"
